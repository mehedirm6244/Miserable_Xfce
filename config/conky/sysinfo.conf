 conky.config =
 {
	update_interval = 1.0,
	total_run_times = 0,
	net_avg_samples = 2,
	cpu_avg_samples = 2,

	double_buffer = true,
	no_buffers = true,

	use_xft = true,
	font = 'noto sans:size=11',
	override_utf8_locale = true,

	default_color = '#dddddd',

	own_window_class = 'Conky',
	own_window = true,
	own_window_type = 'desktop',
	own_window_argb_visual = true,
	own_window_transparent = true,
	own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',

	border_inner_margin = 0,
	border_outer_margin = 0,

	alignment = 'bottom_right',
	gap_x = 40,
	gap_y = 78,
	minimum_width = 200,
	maximum_width = 520,

	draw_shades = false,
};

conky.text = [[
${alignr}CPU
${alignr}${font :size=16}${cpu cpu0}%${font}

${alignr}Memory
${alignr}${font :size=16}${mem}${font}

${alignr}Swap
${alignr}${font :size=16}${swap}${font}
]];
